import React, { Fragment } from "react";
import Header from "../components/Header";

const Login = () => {
  return (
    <Fragment>
      <Header />
      <div>Login</div>
    </Fragment>
  );
};

export default Login;
